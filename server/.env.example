
# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=ai_detection

# Server Configuration
PORT=5000
# Change this to your frontend URL if needed
CORS_ORIGIN=http://localhost:5173

# Authentication
JWT_SECRET=your_jwt_secret_key
AUTH_EXPIRY=24h

# File Analysis Settings
MAX_FILE_SIZE=10485760 # 10MB

# Important Setup Instructions:
# 1. Rename this file to .env
# 2. Update the values with your actual database credentials
# 3. Generate a strong JWT_SECRET (you can use a random string generator)
# 4. Make sure the PORT doesn't conflict with other services
# 5. Start the server with: npm start or node server.js
